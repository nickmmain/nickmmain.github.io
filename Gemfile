source "https://rubygems.org"

# Tell Cloudflare to use the official GitHub Pages bundle
gem "github-pages", group: :jekyll_plugins

# If you use specific Jekyll plugins (like jekyll-feed or jekyll-seo-tag), list them here:
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end